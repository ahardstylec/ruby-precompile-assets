%ul.nav.nav-tabs
  %li=link_to icon_tag(:list, pat(:list)), url(:accounts, :index)
  %li.active=link_to icon_tag(:plus, pat(:new)), url(:accounts, :new)

.tabs-content
  = form_for :account, url(:accounts, :create), :class => 'form-horizontal' do |f|
    =partial 'accounts/form', :locals => { :f => f }
